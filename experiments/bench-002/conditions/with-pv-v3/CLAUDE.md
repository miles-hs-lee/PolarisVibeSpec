# auth-api project notes

Node.js auth API. Before any code change, run `pv ask "<your intent>"` first
and follow the `classification.recommendation` field it returns
(`use_pv` reads `impact.impacted_files` only; `use_grep` skips PV; `use_both`
greps within the impact set).
