# auth-api project notes

This is a small Node.js auth API.

The codebase implements two endpoints — signup and login — backed by an
in-memory user store. Layout follows a typical `src/auth/` and `src/users/`
split.

Make code changes following the existing patterns in the repository.
